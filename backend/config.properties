# ---- DriveEase configuration ----
# Change db.password to your MySQL root password (or use another user).
db.url=jdbc:mysql://localhost:3306/driveease?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
db.user=root
db.password=

port=5000
frontend.dir=frontend
schema.file=database/schema.sql

# Set to true when running behind HTTPS (marks the session cookie "Secure")
production=false
